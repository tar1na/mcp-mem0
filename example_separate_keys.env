# Example configuration with separate API keys for LLM and embeddings
# Copy this to .env and modify as needed

# =============================================================================
# Server Configuration
# =============================================================================
TRANSPORT=sse
HOST=192.168.1.175
PORT=3003

# =============================================================================
# LLM Configuration
# =============================================================================
LLM_PROVIDER=openai
LLM_BASE_URL=http://llm-server.local:8009/v1
LLM_API_KEY=sk-llm-your-llm-api-key-here
LLM_CHOICE=gpt-3.5-turbo

# =============================================================================
# Embedding Configuration
# =============================================================================
EMBEDDING_PROVIDER=openai
EMBEDDING_BASE_URL=http://embedding-server.local:8009/v1
EMBEDDING_API_KEY=sk-embedding-your-embedding-api-key-here
EMBEDDING_MODEL_CHOICE=text-embedding-3-small
EMBEDDING_MODEL_DIMS=1536

# =============================================================================
# Database Configuration
# =============================================================================
DATABASE_URL=postgresql://admin:fer34fer@db-server.local:5432/postgres

# =============================================================================
# Database Connection Management
# =============================================================================
DATABASE_POOL_SIZE=5
DATABASE_MAX_CONNECTIONS=20
DATABASE_MAX_OVERFLOW=10
DATABASE_POOL_TIMEOUT=30
DATABASE_POOL_RECYCLE=3600
DATABASE_HEALTH_CHECK_INTERVAL=60
DATABASE_RETRY_ATTEMPTS=3
DATABASE_RETRY_DELAY=2.0
DATABASE_MAX_RETRY_DELAY=60.0

# =============================================================================
# Configuration Notes
# =============================================================================
# This example shows how to use different servers and API keys for:
# - LLM operations: llm-server.local:8009 with sk-llm-... key
# - Embedding operations: embedding-server.local:8009 with sk-embedding-... key
# - Database: db-server.local:5432
#
# Benefits of this setup:
# 1. Load balancing: Different servers can handle different workloads
# 2. Security: Different API keys for different services
# 3. Scaling: Scale LLM and embedding services independently
# 4. Monitoring: Track usage and costs separately
# 5. Failover: If one service fails, the other can continue working
