# Example configuration for servers that don't require API key authentication
# Copy this to .env and modify as needed

# =============================================================================
# Server Configuration
# =============================================================================
TRANSPORT=sse
HOST=192.168.1.175
PORT=3003

# =============================================================================
# LLM Configuration (No Authentication Required)
# =============================================================================
LLM_PROVIDER=openai
LLM_BASE_URL=http://no-auth-server.local:8009/v1
# Empty API key - server doesn't require authentication
LLM_API_KEY=
LLM_CHOICE=gpt-3.5-turbo

# =============================================================================
# Embedding Configuration (No Authentication Required)
# =============================================================================
EMBEDDING_PROVIDER=openai
EMBEDDING_BASE_URL=http://no-auth-embedding-server.local:8009/v1
# Empty API key - server doesn't require authentication
EMBEDDING_API_KEY=
EMBEDDING_MODEL_CHOICE=text-embedding-3-small
EMBEDDING_MODEL_DIMS=1536

# =============================================================================
# Database Configuration
# =============================================================================
DATABASE_URL=postgresql://admin:fer34fer@db-server.local:5432/postgres

# =============================================================================
# Database Connection Management
# =============================================================================
DATABASE_POOL_SIZE=5
DATABASE_MAX_CONNECTIONS=20
DATABASE_MAX_OVERFLOW=10
DATABASE_POOL_TIMEOUT=30
DATABASE_POOL_RECYCLE=3600
DATABASE_HEALTH_CHECK_INTERVAL=60
DATABASE_RETRY_ATTEMPTS=3
DATABASE_RETRY_DELAY=2.0
DATABASE_MAX_RETRY_DELAY=60.0

# =============================================================================
# Configuration Notes
# =============================================================================
# This example shows how to configure the system for servers that don't require
# API key authentication. This is useful for:
# - Local development servers
# - Internal servers with IP-based access control
# - Servers using other authentication methods (JWT, OAuth, etc.)
# - Public APIs that don't require authentication
#
# Benefits of this setup:
# 1. Simplified configuration - no need to manage API keys
# 2. Development friendly - easy to set up local testing
# 3. Internal networks - can use IP-based security
# 4. Public APIs - access public services without authentication
#
# Security considerations:
# 1. Ensure your servers are properly secured
# 2. Use network-level security (firewalls, VPNs)
# 3. Consider rate limiting on your servers
# 4. Monitor access logs for suspicious activity
